{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('bundles/app/css/actualite.css') }}">
{% endblock %}

{% block body %}
<div class="container-fluid" id="headContainer">
            <h1 class="text-center pt-5 text-white">ACTUALITÉS</h1>
</div>

<div class="container">
   <div class="d-flex flex-wrap justify-content-between row-h1">
       {% for article in articles %}
       <div class="card p-2 m-2 item-h1" style="width: 20rem">
           <img src="{{ asset('bundles/app/img/'~ article.articleImage) }}" alt="image_article" class="card-img-top" style="width: 200px">
           <div class="card-title p4"><h1>{{ article.title }}</h1></div>
           <div class="card-body p4">{{ article.content }}</div>
           <div class="card-footer p4"><a href="#"><button class="btn btn-success">Lire la suite</button></a></div>
       </div>
       {% endfor %}
               
   </div>

</div>

{% endblock %}